ui_mod_sse_character <- function(id) {
 ui_mod_analysis_base(id,'character','Character-dependent diversification',
 controls=shiny::p('Analysis execution is not implemented yet. Prepare and inspect inputs in the Data card.'),
 results=shiny::p('Analysis workspace'),diagnostics=shiny::p('Analysis workspace'),
 mirror=shiny::p('Code will appear when this analysis is implemented.'))
}
